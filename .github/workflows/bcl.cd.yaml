name: "BCL Fx Delivery"

on: 
  workflow_dispatch:
  push:
    paths: 
      - 'bcl2/**'
      #- '.github/workflows/bcl.cd.yaml'
      #- '.github/workflows/multi-target-lib-cd.yaml'
    paths-ignore: 
      - 'lib/Data/**'
    tags: 
      - 'bcl2-*'
    branches:
      - 'main-2'

jobs:
  bcl-cd:
    uses: ./.github/workflows/multi-target-lib-cd.yaml
    with:
        project-dir: "bcl"
        project-name: "bcl"
    secrets: inherit